<?xml version="1.0" encoding="utf-8"?>
<!-- Only the directories shareFile() hands out. filesDir also holds client_certs/, scripts/
     and dcc_partials.json, which must not be reachable through a granted content URI. -->
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <files-path name="logs" path="logs/" />
    <files-path name="dcc" path="dcc/" />
    <cache-path name="dcc_out" path="dcc_out/" />
</paths>
