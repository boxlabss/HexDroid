<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background"/>
    <foreground android:drawable="@mipmap/ic_launcher_foreground"/>
    <!--
        monochrome layer (API 33 / Android 13+): used by themed icon launchers to tint
        the icon with the user's wallpaper color palette.
        Re-uses the foreground shape — launchers apply their own tint via the Material
        You dynamic color system, so the vector here should be a single-color silhouette.
        See: https://developer.android.com/develop/ui/views/launch/icon_design_adaptive
    -->
    <monochrome android:drawable="@drawable/ic_launcher_monochrome"/>
</adaptive-icon>
