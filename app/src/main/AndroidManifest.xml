<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33" />

    <!--
        Android 17 (API 37) requires ACCESS_LOCAL_NETWORK to connect to devices on the
        local network (LAN). This covers:
          - IRC servers/bouncers hosted on a local IP (192.168.x.x, 10.x.x.x, etc.)
          - DCC file transfers and DCC chat (peer-to-peer, often LAN addresses)
        On earlier Android versions this permission is not defined and is simply ignored.
    -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" tools:targetApi="34" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".HexDroidApp"
        android:label="HexDroid IRC"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.HexDroidIRC"
        android:supportsRtl="true"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:localeConfig="@xml/locales_config"
        tools:targetApi="31">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps an IRC connection active for real-time messages when the user enables Keep connection alive; shows an ongoing notification" />
        </service>

        <receiver
            android:name=".NotificationReplyReceiver"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Handle irc:// and ircs:// deep links.
                 Scheme-only matching catches all servers/ports/channels.
                 CATEGORY_BROWSABLE is required for links clicked in a browser to reach this app.
                 irc+ssl:// is also handled as a valid-URI alternative used by HexChat/irssi
                 for SSL connections (avoids the non-standard +port hack that Chrome rejects). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="irc" />
                <data android:scheme="ircs" />
                <data android:scheme="irc+ssl" />
            </intent-filter>
            <!-- Share to IRC: receive text/URLs from other apps via the system share sheet. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

    </application>
</manifest>